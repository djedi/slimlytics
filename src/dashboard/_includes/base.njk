<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Slim Analytics</title>
    
    <!-- Dashboard CSS -->
    <link rel="stylesheet" href="/css/dashboard.css">
    
    <!-- Critical: Configuration must be loaded first -->
    <script src="/js/config.js"></script>
    
    <!-- API Cache for preventing duplicate requests -->
    <script src="/js/api-cache.js"></script>
    
    <!-- Site Manager for persistent site selection -->
    <script src="/js/site-manager.js"></script>
    
    <!-- Alpine.js for interactivity (defer for non-blocking) -->
    <script defer src="/js/alpine.min.js"></script>
    
    <!-- Alpine initialization wrapper with error handling -->
    <script defer src="/js/alpine-init.js"></script>
    
    <!-- Dashboard logic (defer for non-blocking) -->
    <script defer src="/js/dashboard.js"></script>
    
    <!-- HTMX for server interactions (async for non-blocking) -->
    <script async src="/js/htmx.min.js"></script>
    
    <!-- API Diagnostic tool (development only) -->
    <script>
        // Only load diagnostic tool in development
        if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') {
            const script = document.createElement('script');
            script.src = '/js/api-diagnostic.js';
            script.defer = true;
            document.head.appendChild(script);
        }
    </script>
</head>
<body>
    {{ content | safe }}
</body>
</html>